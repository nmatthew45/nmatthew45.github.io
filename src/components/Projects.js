import { Container } from "react-bootstrap";

function Projects() {
  const style = {
    flex: "1",
  };

  return (
    <Container fluid style={style}>
      {/* TODO */}
    </Container>
  );
}

export default Projects;
